Place FFmpeg binaries here (not committed).

This repo's lyric video renderer looks for:
- tools/ffmpeg.exe

Recommended (Windows):
1) Download an FFmpeg "essentials" or "full" build.
2) Copy ffmpeg.exe (and optionally ffprobe.exe) into this folder.

Keeping the exe here avoids needing to modify your system PATH.
